# Asteroid

Jupyter-like JavaScript REPL editor

![](https://media.giphy.com/media/W5g5W5hMkzrJQDpN3P/giphy.gif)

## Subsection

- foo
  - bar

1. one
2. two

```jsx asteroid
export default () => (
  <p style={{ padding: 8, fontSize: 30, background: 'orange' }}>
    Asteroid renders JSX!
  </p>
);

export const x = 'word';
```

```jsx asteroid
import { x } from '.';

export default x;
```

Inline html test <span>foo</span> {<>1 + 1</>}!

<Heading attrs props={1} class="a">Script block test</Heading>

export const x = 1;
