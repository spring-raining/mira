{
  "name": "@mirajs/transpiler",
  "version": "1.0.0",
  "exports": {
    ".": "./lib/index.mjs"
  },
  "types": "./lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "shx rm -rf lib",
    "build": "run-p build:*",
    "build:rollup": "rollup -c",
    "build:types": "tsc -p tsconfig.module.json --emitDeclarationOnly --outDir lib",
    "test": "npm run build && jest"
  },
  "dependencies": {
    "//": "Set the exact version for esbuild-wasm. ~ or ^ will cause a problem.",
    "esbuild-wasm": "0.8.50"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.10.1"
  }
}
